# Build 24 Home and OPAi Assistant UI Correction

## Scope

This is a focused UI correction pass after Build 23 was uploaded to App Store Connect.

## Corrections

- Removed the mint sparkle/star decoration from the Home hero shield block.
- Updated the shared header badge wording to `Canadian Police`.
- Adjusted the header badge width and font sizing so the text fits without clipping.
- Reworked the OPAi Assistant hero card layout so `OPAi Assistant` has a wider text area.
- Reduced the Assistant hero icon size and kept the `MOCK AI` pill compact.
- Updated the default Calendar/training demo item title from `Scenario training` to `Training`.

## Build Note

Build 23 has already been generated and uploaded. Any replacement binary containing these corrections must use Build 24 or higher.

## Integration Limits

No backend, OpenAI, database, payment, analytics, tracking, file upload, police-service integration, Android release workflow, credentials, or production secrets were added.
